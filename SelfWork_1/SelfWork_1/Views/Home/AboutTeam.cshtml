
@{
    ViewBag.Title = "AboutTeam";
}
@using SelfWork_1.Models.TeamPlayer
@model IEnumerable<TeamPlayer>
<div style="display:flex;">
    <div style="justify-content:start">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                @foreach (TeamPlayer item in Model)
                {
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="~/Content/images/Background/instagram.png" alt="Первый слайд">
                    </div>
                }
            </div>
            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>
    <div style="justify-content:end">
        <div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
            <div class="carousel-inner">
                @foreach (TeamPlayer item in Model)
                {
                    <div class="carousel-item active">
                        <img class="d-block w-100" src="~/Content/images/Background/instagram.png" alt="Первый слайд">
                    </div>
                }
            </div>
            <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
            </a>
            <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
            </a>
        </div>
    </div>
</div>